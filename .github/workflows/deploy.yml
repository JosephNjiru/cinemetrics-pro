# CineMetrics Pro - Updated GitHub Pages Deployment
# Dr. Joseph N. Njiru, PhD, M PredAnylt (Data Science), MEd, GD Stat, GC HigherEd, GC-GEOSPI
# Advanced Film Intelligence & Awards Analytics Platform
# Updated: 2025-08-24 15:53:24 UTC

name: 🎬 Deploy CineMetrics Pro to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4
      
    - name: ⚙️ Setup Node.js 18
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: |
          package-lock.json
          frontend/package-lock.json
          backend/package-lock.json
        
    - name: 🔧 Setup Environment (Disable External Services)
      run: |
        node ./scripts/setup-env.js
        node ./scripts/block-phantom-scripts.js
        echo "SNYK_TOKEN=false" >> $GITHUB_ENV
        echo "DISABLE_SCARF=true" >> $GITHUB_ENV
        echo "SCARF_ANALYTICS=false" >> $GITHUB_ENV
        echo "SCARF_DISABLE=true" >> $GITHUB_ENV
        echo "SENTRY_DISABLE=true" >> $GITHUB_ENV
        echo "DISABLE_OPENCOLLECTIVE=true" >> $GITHUB_ENV
        
    - name: 📦 Install Frontend Dependencies
      env:
        SNYK_TOKEN: false
        DISABLE_SCARF: true
        SCARF_ANALYTICS: false
        SCARF_DISABLE: true
        SENTRY_DISABLE: true
        DISABLE_OPENCOLLECTIVE: true
        npm_config_fund: false
        npm_config_audit: false
      run: |
        cd frontend
        npm ci --prefer-offline --no-audit --no-fund || npm install --no-audit --no-fund
        
    - name: 🏗️ Build React Application
      env:
        PUBLIC_URL: /cinemetrics-pro
        REACT_APP_ENVIRONMENT: production
        REACT_APP_VERSION: 1.0.0
        CI: false
        SNYK_TOKEN: false
        DISABLE_SCARF: true
        SCARF_ANALYTICS: false
        SCARF_DISABLE: true
        SENTRY_DISABLE: true
        DISABLE_SENTRY: true
        DISABLE_OPENCOLLECTIVE: true
        npm_config_fund: false
        npm_config_audit: false
      run: |
        cd frontend
        npm run build
        
    - name: 📄 Setup Pages
      uses: actions/configure-pages@v4
      
    - name: 📤 Upload Build Artifacts
      uses: actions/upload-pages-artifact@v3
      with:
        path: frontend/build
        
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    
    steps:
    - name: 🚀 Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
      
    - name: ✅ Deployment Success
      run: |
        echo "🎬 CineMetrics Pro build completed successfully!"
        echo "🌐 Repository: https://github.com/JosephNjiru/cinemetrics-pro/"
        echo "📁 Built files available in frontend/build directory"
